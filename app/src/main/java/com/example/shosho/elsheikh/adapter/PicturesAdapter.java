package com.example.shosho.elsheikh.adapter;

public class PicturesAdapter {
        /*extends RecyclerView.Adapter<ItemsAdapter.ViewHolder> {
    private Context context;
    //........
    //......
    @NonNull
    @Override
    public ItemsAdapter.ViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
        View view= LayoutInflater.from( context ).inflate( R.layout.row_items,parent,false);
        return new ViewHolder(view);
    }

    @Override
    public void onBindViewHolder(@NonNull ItemsAdapter.ViewHolder holder, int position) {

    }

    @Override
    public int getItemCount() {
        return 0;
    }

    public class ViewHolder extends RecyclerView.ViewHolder {
        private ImageView imageView;
        private TextView textName;

        public ViewHolder(View itemView) {
            super( itemView );
            imageView=itemView.findViewById( R.id.row_items_image );
            textName=itemView.findViewById( R.id.row_items_names );
        }
    }*/
}
